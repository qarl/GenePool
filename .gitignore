# macOS
.DS_Store
._*

# editor / temp
*.swp
*~

# run-recorder default outputs (written to the cwd) -- run artifacts, not source
run.jsonl
run.db
viewer-micrograph-params.json
viewer-swimmer.json

# visual-goldens harness: isolated deps + transient diff artifacts (goldens/*.png ARE committed)
test/visual/node_modules/
test/visual/goldens/*.diff.png

# scrub/playback per-seed run databases (regenerable cache)
runs/

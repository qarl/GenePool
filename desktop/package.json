{
  "name": "genepool-desktop",
  "productName": "GenePool",
  "version": "0.0.0",
  "private": true,
  "description": "GenePool standalone desktop app (Electron): the micrograph viewer + real-file save/load + SQLite snapshot recording, all in one process.",
  "type": "module",
  "main": "main.mjs",
  "scripts": {
    "start": "electron .",
    "package": "bash build-app.sh"
  },
  "devDependencies": {
    "@electron/packager": "^20.3.0",
    "electron": "^44.2.0"
  }
}
